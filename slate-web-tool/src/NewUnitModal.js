import { React } from 'react';

const NewUnitModal = (props) => {
	
	const {
		setDisplayModal,
		handleClose,
		newUnit,
		editNewUnitName,
		editNewUnitStats,
		editNewUnitPsychic,
		editNewUnitShooting,
		editNewUnitCombat,
		editNewUnitKeyWords
	} = props;

	return (
		<Modal>
			
		</Modal>
	);
};